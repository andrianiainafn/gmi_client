import React from 'react';
import {TabsContent} from "@/components/ui/tabs";

const NotificationReportContainer = () => {
    return (
        <TabsContent value="report">Report.</TabsContent>
    );
};

export default NotificationReportContainer;